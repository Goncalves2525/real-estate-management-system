@startuml
skinparam monochrome true
skinparam packageStyle rectangle
skinparam shadowing false

title System Sequence Diagram (SSD)

autonumber

actor "System Administrator" as Admin
participant ":System" as System

activate Admin

    Admin -> System : requests to import a csv file
    activate System

'    System --> Admin : lists types of data to import (Agents, Agencies, Orders)\n and asks to select one
'    deactivate System
'
'    Admin -> System : selects a type of data to import
'    activate System

    System --> Admin : requests to insert csv file location
    deactivate System

    Admin -> System: selects csv file location
    activate System

    System --> Admin: displays the amount of data found to import \nand the success message
    deactivate System

'    Admin -> System: selects the delimiter
'    activate System

'    System --> Admin: displays the data that it found in the csv file
'    deactivate System

'    Admin -> System : confirms the displayed data is the one to import
'    activate System
'
'    System --> Admin : displays success message
'    deactivate System


@enduml