@startuml
skinparam monochrome true
skinparam packageStyle rectangle
skinparam shadowing false

title System Sequence Diagram (SSD) - Alternative Two

autonumber

actor "Owner" as Owner
participant ":System" as System
participant ":Agent" as Agent

activate Owner

        Owner -> System : displays list of Agencys
    activate System

        System --> Owner :submits selected Agency
    deactivate System

        Owner -> System :displays list of Agent
    activate System

       System --> Owner : submits selected Agent
    deactivate System

        Owner -> System : Submit a request for listing a property for sale or rent
    activate System

        System --> Owner : requests Owner data
    deactivate System

        Owner -> System : types requested data (name, citizen's card number, \ntax number, address, email address, \ncontact telephone number).
    activate System

        System --> Owner : requests type of request
    deactivate System


        Owner -> System : types requested data (Sale or Rent)
    activate System


    alt if Rent

        System -> Owner: request contract duration
        deactivate System
    else

        Owner -> System: types requested data
        activate System
    end

        System --> Owner : requests type of property
    deactivate System

        Owner -> System : types requested data (Apartement, House, Land)
   activate System

            System --> Owner : requests data of property
    deactivate System

          Owner -> System : Select requested data
    activate System

        System --> Owner : displays operation success


        System --> Agent : Submit property
    deactivate System

@enduml